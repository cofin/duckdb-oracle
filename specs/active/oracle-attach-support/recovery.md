# Recovery Plan

If the session is interrupted, resume by:

1. Checking `specs/active/oracle-attach-support/tasks.md` for progress.
2. If implementation has started, check `src/` for new files (`oracle_catalog.cpp`, etc.).
3. Continue with the next unchecked task.
